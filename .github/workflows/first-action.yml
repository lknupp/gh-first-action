name: First Workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!!!"
      - name: Sleep for 5 seconds
        run: sleep 5
      - name: Print goodbye
        run:  echo "Done - bye!!!"
  
